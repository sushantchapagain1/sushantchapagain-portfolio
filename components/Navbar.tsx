import React from 'react';
import { LogoDarkIcon } from './icons';

function Navbar() {
  return (
    <header>
      <LogoDarkIcon height={40} width={40} />
    </header>
  );
}

export default Navbar;
